---
status: historical
owner: governance
truth_level: historical-reference
last_verified_commit: f56ac30
---

> **Arkivnot (lane A, 2026-06):** Historisk pulskoll — behållen som referens (ej
> sanningskälla). Nuläge: `docs/current-focus.md`. Se `docs/archive/README.md`.

# Halvtidsvila — repo-pulskoll 2026-05-25 (eftermiddag)

Operatör bad om en stämma-av-rapport efter att backup-branchen
`backup-44-BRA` skapats från `jakob-be` (`41db367`). Allt nedan är fakta
från live-körningar plus läsning av kanoniska docs idag 2026-05-25 ~17:15
lokal tid. Rapporten är medellång (~15 min spann); inte en full audit.

## TL;DR

- **Hälsa är grön över hela linjen.** Lint 0, tester 1454 pass / 6 förväntade
  skips, governance 18/18, rules-sync OK, term-coverage strict OK,
  sprintvakt-check OK. `jakob-be` och `main` är innehållsmässigt identiska
  efter dagens sync-PR (#103).
- **Volymen är ovanlig.** 452 commits på 18 dagar (~25/dag), 179 senaste 7
  dagarna. 30 ADRs, 22 governance-regler, 18 policies, 56 markdown-filer
  i docs/, 26 Python-skript.
- **Den verkliga risken syns i commit-fördelningen.** 35 av senaste 100
  commits är `docs(steward)`. En tredjedel av all aktivitet är
  koordination/dokumentation snarare än produktyta. Förra agenten
  beskrev själv produktloopen som "5–6/10" trots commit-volymen
  (`mer_info.txt:17`).
- **Tre konkreta städkandidater idag** (utan att röra produktkod):
  redundanta cleanup-scripts, daterad backend-handoff och oavstängda
  handover-filer på disk.
- **Två beslut som hänger i luften** ska tas innan launch eller innan
  nästa stora sprint: **B125 preview-fallback-ADR** (Hög, produktblocker)
  och **B13a-flytt** (5696-radersfilen).

---

## 1. Hälsostatus (live-verifierad just nu)

| Kontroll | Kommando | Resultat |
|---|---|---|
| Lint | `python -m ruff check .` | All checks passed |
| Tester | `python -m pytest tests/ -q` | 1454 pass + 6 skips, 187 s |
| Governance | `python scripts/governance_validate.py` | 18 policies OK |
| Rules-sync | `python scripts/rules_sync.py --check` | speglar synkade |
| Termcoverage | `python scripts/check_term_coverage.py --strict` | OK |
| Sprintvakt | `python scripts/sprintvakt_check.py` | OK |
| Bug-scope | `python scripts/list_open_bugs.py` | 20 aktiva / 5 unknown / 112 stängda |

De 6 förväntade test-skippen är: 3× Windows-symlink (privilegier), 2× LLM
E2E (kräver `SAJTBYGGAREN_E2E=1` + `OPENAI_API_KEY`), 1× långsam
npm-bygg-verifiering (kräver `SAJTBYGGAREN_VERIFY_BUILD=1`). Alla är
medvetna gates, inga regressions.

## 2. Bug-state idag

`python scripts/list_open_bugs.py` (källa: `docs/known-issues.md`):

- **20 aktiva** (Fix: open).
- **5 unknown** — Öppna-poster utan tydlig Fix-markör som behöver
  operatör-triage: BO4-followup-cancel, B13a, B47, B49, B53.
  Inga av dem är blockare för dagens flöden.
- **112 stängda** med regressionstester.
- **0 misplaced.**

Aktiva uppdelade på allvar:

- **Hög (1):** `B125` — Safari/Firefox preview-fallback. Produktblocker
  innan launch. ADR-beslut behövs.
- **Medel (3):** `B59` (StackBlitz template-node-preview), `B89`
  (language-detection default), `B91` (location-hint normalization).
- **Låg (16):** mestadels copy/render-edge-cases i `build_site.py` och
  `prompt_to_project_input.py` — alla i B13a-zonen.

Misstänkt felklassad: B116 (global build-mutex) bör vara stängd efter
PR #100 (per-siteId Map). Värt en operatör-koll i `known-issues.md`.
(Uppdatering: B116 flyttad till Stängda 2026-05-25 eftermiddag med
Fix `51c1d19`.)

## 3. Volym- och fördelningsbild

```text
Repo-ålder:          18 kalenderdagar (initial commit 2026-05-07)
Commits totalt:      452
Commits senaste 7d:  179
ADRs:                30 (0001–0030)
Governance-regler:   22 (alla speglade till .cursor/rules/)
Policies:            18 (alla schemavaliderade)
Docs (markdown):     56 filer
Python-scripts:      26
```

### Commit-fördelning senaste 100

```text
35  docs(steward)
 8  fix(viewser)
 5  fix(builder)
 4  fix(backoffice)
 4  feat(backoffice)
 3  fix(grind)
 3  feat(builder)
 2  feat(tooling)
 2  chore(tooling)
 2  docs(adr)
 2  feat(viewser)
 2  fix(rules-sync)
```

**Observation:** `docs(steward)` är 3× större än näst största kategorin.
Förra agenten skrev själv i `mer_info.txt:19`: "kärnflödet är 5–6/10 trots
443 commits — mycket av tiden går till koordinationslager, inte
produktyta." Det här är inte fel i sig (governance-disciplinen är
mätbart värdefull — bug-räkningen 145→112-stängd visar att audits faktiskt
konverterar), men det förklarar varför produktupplevelsen inte
accelererar i samma takt som commit-räkningen.

## 4. Dokumentationsdrift (måste fixas snart)

Tre docs har "Last verified state"-SHA:r som är äldre än verkligheten:

| Fil | Hävdad state | Verklig HEAD | Drift |
|---|---|---|---|
| `docs/current-focus.md` | `2a5d2e5` (morgon 5/25) | `41db367` (kväll 5/25) | 4 PRs + sync-PR senare |
| `docs/handoff.md` | `2a5d2e5` (morgon 5/25) | `41db367` (kväll 5/25) | samma |
| `README.md` ADR-lista | slutar på 0028 | 0030 finns | 2 ADRs saknas |

`handover-2026-05-25-merge-complete.md` på disk har den färska bilden
men är gitignored (transient agent-fil) och kommer raderas. Risken: nästa
agent som följer "läs `docs/current-focus.md` först"-regeln får en
felaktig världsbild — den tror att Sprintvåg 1+2 inte är mergad än, fast
allt sitter i main.

Steward-rollen är specifikt definierad för att hålla dessa filer aktuella
(se `governance/rules/team-workflow.md` + `docs/agent-handbook.md`-loop
steg 8). Att den ändå driftar betyder antingen (a) någon agent merge:ade
utan att köra Steward-loopen, eller (b) Steward-disciplinen bryts under
parallell-sprintar. Båda är värda att verifiera.

## 5. Städkandidater (säkra att rensa nu)

### 5.1 Cleanup-scripts dubbelarbete

Tre verktyg för i princip samma jobb:

| Skript | Rader | Vad det gör |
|---|---|---|
| `scripts/prune_generated_previews.py` | 553 | Prunar `../sajtbyggaren-output/.generated/<siteId>/` med retention-caps + current-pointer-skydd |
| `scripts/cleanup_dev_artifacts.py --generated` | 367 | Samma men kombinerat med eval/python-cache-cleanup |
| `scripts/clean-runs.ps1` | — | PowerShell-wrapper för `data/runs/`-cleanup |

`README:124-128` försöker delineera dem ("`cleanup_dev_artifacts.py` är
den säkrare samlade dev-cleanupen") men två Python-scripts för
samma grundjobb är aldrig en hållbar struktur. **Förslag:** behåll
`cleanup_dev_artifacts.py` (yngre, mer modulär), flytta den unika
retention-cap-logiken från `prune_generated_previews.py`, radera den.

### 5.2 Eval-scripts oklart territorium

- `scripts/mini_eval.py` (417 rader) — "isolerad fyra-case mini-eval"
- `scripts/run_eval_suite.py` (481 rader) — `quick`/`full` mot
  on-disk-scaffolds

De har olika scope idag men är förvirrande nära varandra. Ingen akut
åtgärd, men värt en `docs/evals.md`-läsning för att kontrollera att
nuvarande indelning fortfarande är motiverad eller om en enad runner
hade räckt.

### 5.3 Daterade handoff-filer i docs/

- `docs/backend-handoff.md` — kanonisk
- `docs/archive/backend-handoff-2026-05-22.md` — daterad, scope-historik (arkiverad 2026-05-27)
- `docs/archive/2026-05-19/scout-orchestrator-handoff-2026-05-19.md` — arkiverad 2026-05-26

De daterade fyller en historisk roll men är inte hänvisade från
`current-focus.md` eller `handoff.md` längre. **Förslag:** flytta till
`docs/archive/<datum>/` eller radera om innehållet är inkapslat i nyare
docs. Samma fråga gäller `docs/reports/`-mappen (6 daterade Scout-rapporter
från 5/19–5/25) — dokumentationsvärde finns men signal-brus-förhållandet
sjunker.

### 5.4 Handover-filer på disk (gitignored, transient)

- `mer_info.txt` (förra agentens session-handover, mycket bra kontext)
- `handover-stackblitz-2026-05-25.md` (löst i PR #101)
- `handover-2026-05-25-merge-complete.md` (idag, sync-summa)

Alla tre är `.gitignore`:ade (regel: `handover-*.md` + ad-hoc för
`mer_info.txt` via untracked). Operatör kan radera när som helst. Förslag:
behåll `handover-2026-05-25-merge-complete.md` tills nästa sessions agent
har läst den (den har den färska bilden Steward-docs saknar); de andra
två kan tas bort.

### 5.5 Agent-prompts partial migration

- `docs/agent-prompts.md` — single-file katalog (Scout/Builder/Steward)
- `docs/agent-prompts/` — mapp med `morning-fresh-start.md`,
  `sprintvakt.md`

Risk: dubbel-läsa eller att rollerna driftar. **Förslag:** välj struktur —
antingen en fil eller en mapp per roll — och migrera fullt ut.

## 6. Dubbel-logik i produktkoden (känd och ny)

### 6.1 Känd: `_normalize_business_type` split-sanning (B110)

`scripts/build_site.py:_normalize_business_type` normaliserar
`naprapath`/`naprapatklinik`/`webshop`-varianter, men bara i CTA-flödet.
`scripts/prompt_to_project_input.py` har egna mappar
(`_BUSINESS_TYPE_LABEL_SV`, `_TAGLINE_BY_BUSINESS_TYPE_SV`, etc.) som
nyckel direkt på rå briefModel-output. Samma input renderar olika i
olika rendering-steg. Spårad som B110 (Låg-Medel) sedan 2026-05-18,
fortfarande öppen.

### 6.2 Känd: cwd-antaganden i viewser/lib (R1#1 + R2#3)

Sju filer i `apps/viewser/lib/` anropar `process.cwd()`:
`local-preview-server.ts`, `runs.ts`, `build-runner.ts`,
`project-inputs.ts`, `prompt-runner.ts`, `scrape-runner.ts`,
`asset-store/local.ts`. Större strukturell refactor. Förra agenten
flagga: "behöver dedikerad sprint med scout först. Inte blockare."

### 6.3 Känd: `scripts/build_site.py` arkitektur-skuld (B13a)

**5696 rader, 100 funktioner/klasser.** Detta är *the* megafile. Pathen
`packages/generation/build/` är till och med reserverad i
`.cursorignore` så ingen ska råka få in den till `!packages/...`-trapen
i förväg. Men ingen tid är bokad för flytten. Med 9 av 20 aktiva buggar
(B67, B86, B89, B90, B91, B92, B93, B110, B129) som rör den här filen
blir varje fix dyrare ju längre flytten skjuts på framtiden.

### 6.4 B116 — flyttad till Stängda 2026-05-25 eftermiddag

`docs/known-issues.md` listar B116 (global build-mutex i
`apps/viewser/lib/build-runner.ts`) som öppen, men PR #100 som mergades
idag heter "Per-siteId build mutex (Map ersätter global inFlight)". Värt
en kontroll: ska B116 flyttas till stängd-sektionen med Fix-SHA?

## 7. Stora dokumentfiler — risk vs nytta

| Fil | Rader | Roll | Kommentar |
|---|---|---|---|
| `docs/known-issues.md` | 2481 | Bug-ledger | Aktivt + nödvändigt. Format-disciplin gör att list-skriptet funkar. |
| `docs/current-focus.md` | 1105 | Aktuell köplan | Driftar idag (se §4). Risk att den växer till "alltid-läs-allt" om den inte rensas mellan sprintar. |
| `scripts/build_site.py` | 5696 | Builder | B13a-skuld. Se §6.3. |
| `scripts/prompt_to_project_input.py` | 2666 | Brief→Project-Input | Lever symbiotiskt med build_site.py:s `_normalize_business_type`. |

`current-focus.md` är värd en uppmärksam läsning vid nästa session —
mycket av historiken kan rimligtvis arkiveras nu när 4 PR:er och en
sync är klara. Behåll bara nuläge + nästa-spår, flytta historik till
`docs/archive/2026-05/`.

## 8. Vad som följs väl

Värt att namnge för balansens skull:

- **Ruff på 0 findings.** Tidigare baseline, hållits sedan e8143cf.
- **Rules-sync som obligatorisk gate.** `governance/rules/` → `.cursor/rules/`
  speglar via `scripts/rules_sync.py`. Pre-commit-vana etablerad.
- **Bug-disciplinen.** Format `<ID> – <Allvar> – beskrivning. Källa. Fix.
  Test.` håller, och `list_open_bugs.py` parsar den. 112 stängda buggar
  har regressionstester. Det är ovanligt välhanterat.
- **ADR-takt och kvalitet.** 30 ADRs på 18 dagar, alla numrerade
  konsekvent, ingen lucka. ADR 0030 (preview/deploy-adapters) är ett
  bra exempel på att operatörens "Vercel som adapter, inte beroende"-
  policy nu är låst.
- **Engine Run-artefaktkontraktet.** 8 artefakter per `runId`, trace.ndjson
  append-only, schema-validerat. Test `test_all_eight_engine_run_artifacts_present`
  låser invarianten.
- **Scout/Builder/Steward-rollerna.** Tydlig separation, prompts dokumenterade,
  RO-läge för Scout. Fungerar i praktiken.

## 9. Frågor som rimligtvis ska upp på operatörsbordet

1. **B125 ADR (Hög, produktblocker).** Fyra alternativ är dokumenterade
   (server-byggd static / lokal `next dev`-process per kund / "Öppna i
   StackBlitz"-länk-fallback / Vercel preview deployments). Du har redan
   sagt i ADR 0030 att Vercel ska vara adapter, inte beroende. Vilken
   väg väljer du? Beslutet blockerar pre-launch.

2. **B13a-flytt (5696-radersfilen).** Boka in en sprint nu eller låt det
   ligga? Min observation: 9 av 20 aktiva buggar lever i den filen.
   Varje vecka skuld kostar mer.

3. **Christopher-spåret.** `christopher-ui` är 6 commits framför
   GAMLA main (`51b70bc`), bakom NYA main (`7b7263a`). Han behöver
   merge:a/rebase:a och lösa konflikter i `viewer-panel.tsx`. Vill du
   jaga, eller väntar du tills han hör av sig?

4. **Koordinationsoverhead.** 35% docs(steward)-commits senaste 100.
   Är det överoptimerat? Konkret förslag: prova att hoppa
   `current-focus.md`-bumpar för ren mikrostatus (regelpunkten finns
   redan i `current-focus.md:26`-"Uppdatera inte för ren mikrostatus")
   och se om signal/brus förbättras.

5. **Eval-runners (mini_eval vs run_eval_suite vs quick/full).** Tre
   verktyg, oklart om alla tre behövs. Värt 30 minuter att läsa
   `docs/evals.md` och bestämma.

6. **Cleanup-scripts (3 verktyg, 1 jobb).** Konsolidera till
   `cleanup_dev_artifacts.py`?

7. **Agent-prompts struktur (`.md` vs mapp).** Välj en, migrera klart.

8. **Daterade handover-/report-filer.** Etablera en
   `docs/archive/<YYYY-MM>/`-konvention så docs/ inte växer förbi
   100 markdown-filer på 6 månader.

9. **Lane 2 LLM contract propagation** (PRIO 1 i förra handoffen).
   Fortfarande "ENDA substantiella produktlyftet kvar" enligt
   parallella agenten. Vill du resumea
   `cursor/jakob-be-llm-contract-propagation` eller är prio:n flyttad?

10. **`docs/current-focus.md`-arkivering.** Filen är 1105 rader nu.
    Boka 30 min för att trimma den till "nuläge + nästa-spår", flytta
    historiken till `docs/archive/2026-05/current-focus-history.md`.

## 10. Vad jag inte rört (medvetet)

- `docs/current-focus.md`, `docs/handoff.md`, `docs/workboard.json` —
  Steward-managed, inte min lane utan operatör-direktiv.
- Något under `apps/viewser/` — kräver ändå CI och Christopher-koord.
- Bug-listan i `docs/known-issues.md` — flytta-mellan-sektioner kräver
  Steward-precision.
- `mer_info.txt` på disk — operatör äger den.

## 11. Min rekommendation om nästa drag (väg val)

Om jag fick välja **ett** spår för imorgon, baserat på allt jag sett:

> **Boka 90 minuter för B125-ADR + docs/current-focus.md-bump till
> faktisk HEAD (`41db367`).** Det är två operatör-tunga uppgifter där
> mänskligt beslut + Steward-disciplin är värt mer än kod. Båda öppnar
> sedan för att produkt-lanes (Lane 2 + B13a-flytt) kan köras med tydlig
> riktning.

Alternativt: om du vill ha *kod*-lyft först, plocka **Lane 2 LLM
contract propagation** på `cursor/jakob-be-llm-contract-propagation`.
Förra agenten flaggade explicit den som "ENDA substantiella produktlyftet
kvar".

---

**Bakgrund:** Rapporten skriven av Cursor-agent (Claude Opus 4.7)
2026-05-25 ~17:15 lokal tid mot `jakob-be` HEAD `41db367`. Backup
`backup-44-BRA` skapad från samma SHA före rapportskrivning. Inga
ändringar gjorda i produktkod eller Steward-managed docs. Källor:
live-körningar av health-skript, läsning av `mer_info.txt`,
`handover-2026-05-25-merge-complete.md`, `handover-stackblitz-2026-05-25.md`,
`README.md`, `docs/current-focus.md`, `docs/handoff.md`,
`docs/known-issues.md`, samt git log/diff-snutter.
