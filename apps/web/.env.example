# apps/web — publik UI för Sajtbyggaren.
# Inga server-side secrets behövs i nuläget; sidan är ren UI/UX-import
# från Sajtmaskin_Genberg utan API-routes, drizzle eller auth.

# Bas-URL för canonical/sitemap. Lämna tom under dev så blir det http://localhost:3001.
NEXT_PUBLIC_BASE_URL=
