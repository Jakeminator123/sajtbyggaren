"""Shared helpers and fixtures for the test suite.

Modules under ``tests.support`` hold constants and helper functions that are
reused across multiple test files. Keeping them here avoids the ad hoc
duplication that previously accumulated inside oversized monolithic test
files (see ``tests/test_test_hygiene.py`` for the size guardrail).
"""
