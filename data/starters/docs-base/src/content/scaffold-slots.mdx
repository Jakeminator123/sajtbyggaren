# Scaffold slots

This starter reserves space for education routes that are owned by the
`course-education` scaffold.

## Reserved routes

- `/program/[id]`
- `/lectures`
- `/resources`

The starter does not implement these top-level routes. Keeping them out of the
base prevents conflicts when scaffold generation adds real route handlers and
page content.
