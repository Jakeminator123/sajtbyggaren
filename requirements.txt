# Backoffice uses the modern width="stretch"/"content" API (st.dataframe,
# st.button, st.form_submit_button). width="stretch" for buttons landed in
# 1.48 and for dataframe in 1.49; use_container_width was removed after
# 2025-12-31, so the floor must guarantee the new API (Codex-fynd 2026-06-01).
streamlit>=1.49
jsonschema>=4.23
pydantic>=2.9
# code uses the v2 Responses API (client.responses.parse); cap <3 until a v3 migration is vetted
openai>=2.41,<3
pillow>=10.0

# Operator-only tool: scripts/component_intake.py spawns its OWN
# `npx shadcn@latest mcp` stdio server via the OpenAI Agents SDK pattern
# (agents.mcp.MCPServerStdio) for curated component intake. PINNED exactly per
# operator decision (2026-06-12): this is the operator Python layer, not a
# user-site npm dep (the TOOLS.md npm ban does not apply). The import is lazy in
# the CLI (fast start + honest error outside the venv); CI tests mock the MCP
# session and never import or call it.
openai-agents==0.17.5

# discovery wizard URL-scrape (scripts/scrape_site.py)
requests>=2.32
beautifulsoup4>=4.12

# dev tooling (also installable via `pip install -e .[dev]`)
pytest>=8.3
pytest-cov>=5.0
# parallel full-suite runs: python -m pytest tests/ -q -n auto (docs/testing.md)
pytest-xdist>=3.8
ruff>=0.6
